.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc

# PyInstaller-Build-Artefakte (die EXE wird lokal/Release gebaut, nicht eingecheckt)
build/
dist/
*.spec

# Claude-Arbeitsordner
.claude/

# Persönliches Verknüpfungs-Icon (Xharig-Avatar) — gehört nicht ins geteilte Tool
avatar.ico
_avatar_src.png

# Interne Planung und Notizen gehören nicht ins Repo
docs/

# Entwuerfe fuer kommende Fassungen — bleiben lokal, damit sie nichts vorwegnehmen
tools/entwurf_*.py

# Private Uebergabe-Notiz — gehoert NIE ins oeffentliche Repo (29.08.2026)
UEBERGABE.md
*UEBERGABE*.md
CLAUDE.md

# ⛔⛔ Die Suchbegriffe des Datenschutz-Scanners (11.09.2026).
#
# Eine Sperrliste IM Repo waere die Liste dessen, wonach zu suchen sich lohnt —
# genau verkehrt herum. `tools/privacy_scan.py` prueft als Erstes, dass diese
# Zeile hier steht, und verweigert sonst den Dienst.
.sperrliste
tools/bericht-worker/.wrangler/
tools/bericht-worker/node_modules/
